{
  "name": "fakeborn",
  "version": "1.0.0",
  "description": "Generate fake data born from your validation schema — pass a validator (Zod, Valibot) to fake() and get a value that satisfies it.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node --external @faker-js/faker && bun run build:types",
    "build:types": "tsc -p tsconfig.build.json --emitDeclarationOnly --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "check": "bun run lint && bun run typecheck && bun test"
  },
  "dependencies": {
    "@faker-js/faker": "^9.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxlint": "1.69.0",
    "typescript": "^5.5.0",
    "zod": "^3.23.0"
  },
  "keywords": [
    "faker",
    "fake",
    "mock",
    "test-data",
    "zod",
    "valibot",
    "schema",
    "validator"
  ],
  "author": "",
  "license": "ISC"
}
