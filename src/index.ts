// Public entry point for fakeborn.
//
// The library surface (`fake`, the validator adapters, the IR, the faker map)
// lands with the tracer in #10, which rebases onto this infra foundation. This
// stub keeps `bun run build` and `bun run typecheck` honest until then.
export {};
